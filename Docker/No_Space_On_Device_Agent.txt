systemctl stop docker.socket
systemctl status docker
rm -rf /var/lib/docker
systemctl start docker
/etc/init.d/restart_slave.sh

docker exec -it 1ab034b39edd bash

#Login to slaves:
ssh -i 'id_rsa_jenkins_ptdadmin_sit_gherkins 4' ptdadmin@100.100.100.100

#Check disk space
df -kh

#Remove containers:
docker rm $(docker ps -aq | grep -v $(docker ps -q -f name=worker))
docker ps $(docker ps -aq | grep -v $(docker ps -q -f name=worker))
 
#Remove  volumes:
docker volume rm  $(docker volume ls)
 
#Remove images:
docker rmi -f $(docker images -q) | grep -v [id worker image]
 
#Prune system
docker system prune -af

#Restart slave:
/etc/init.d/restart_slave.sh